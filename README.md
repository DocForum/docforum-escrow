# docforum-escrow
Rust Soroban smart contract (create/release/refund escrow) + a thin TypeScript client SDK wrapping it. No Postgres, no PHI, no healthcare domain logic at all — deliberately generic, same category as Trustless Work's repo.
